#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main(){
    vector<int>A = {1, 4, 3, 4};
    int i=0, j=0;
    int leftspl=-1, rightspl=-1;

    for (i=1, j=0; i>j && i<A.size(); i++, j++) {
        if (A[j]>A[i]) {
            
        }
    }
}