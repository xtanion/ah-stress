#include<bits/stdc++.h>
using namespace std;


int main(){
    vector<int> A{1, 2, 1, 2, 3};
    int B = 2;

    int i, j=1, elements, diff=1, count=0;

    while(j<A.size()){
        elements = 0;
        count = 1;
        if (A[i] != A[j]){

        }
    }
}

